# GitHub Actions Run 22220002065 错误分析报告

**日期**: 2026-02-20  
**运行ID**: 22220002065  
**触发方式**: 工作流调度（workflow_dispatch）  
**运行状态**: ❌ 已取消（用户手动取消）  
**失败时间**: ~15 分钟后  

---

## 1. 执行摘要

所有 8 个矩阵作业均失败。关键发现：

| 作业 | 运行时长 | 失败原因 | 根本问题 |
|-----|--------|--------|--------|
| **linux** 🔴 | 2m49s | 编译失败 | `fatal error: fuse.h` |
| **linux_386** 🔴 | 9m51s | 测试失败 | 库文件缺失 |
| **windows** 🔴 | 15m29s | 未知 | 下游失败 |
| **other_os** 🔴 | 15m29s | 未知 | 下游失败 |
| **go1.24** 🔴 | 15m29s | 未知 | 下游失败 |
| **mac_amd64** 🔴 | 6m2s | 未知 | 下游失败 |
| **mac_arm64** 🔴 | 3m2s | 未知 | 下游失败 |
| lint 🔴 | 0s | 跳过 | 构建失败 |
| android-all 🔴 | 0s | 跳过 | 构建失败 |

**主要失败点**: Linux 作业在 "Build rclone" 步骤失败  
**直接原因**: C 编译器找不到 `fuse.h` 头文件  

---

## 2. 详细错误分析

### 2.1 Linux Job (64272764496) - 主要故障源

**执行流程**:
```
✅ Checkout           (v1.74.2)
✅ Install Go         (go1.26.0)
✅ Set env variables  (GOTAGS=cmount, BUILD_FLAGS='-include "^linux/"')
⚠️  Install Libraries (部分成功)
❌ Build rclone       (fuse.h 未找到)
```

#### 库安装步骤输出分析

**执行的命令**:
```bash
sudo apt-get install -y fuse3 libfuse-dev rpm pkg-config git-annex git-annex-remote-rclone nfs-common
```

**DNF 包管理器执行日志** (来自 GitHub Actions 日志):
```
软件包 fuse3-3.10.2-9.el9.x86_64 已安装。                 ✅ 成功
未找到匹配的参数: libfuse-dev                           ❌ 失败
软件包 rpm-4.16.1.3-39.el9.x86_64 已安装。              ✅ 成功
软件包 pkgconf-pkg-config-1.7.3-10.el9.x86_64 已安装。 ✅ 成功
未找到匹配的参数: git-annex                            ⏭️  被忽略
未找到匹配的参数: git-annex-remote-rclone             ⏭️  被忽略
未找到匹配的参数: nfs-common                           ❌ 失败
```

#### 关键发现

**安装结果统计**:
| 包名 | 请求名称 | Rocky 映射 | 安装状态 | 备注 |
|-----|---------|----------|--------|------|
| fuse3 | fuse3 | (未映射) | ✅ | 直接命中 |
| libfuse-dev | libfuse-dev | fuse3-devel | ❌ | **映射失败** |
| rpm | rpm | rpm | ✅ | 直接命中 |
| pkg-config | pkg-config | pkgconf-pkg-config | ✅ | 映射成功 |
| git-annex | git-annex | (忽略) | ⏭️  | 在忽略列表 |
| git-annex-remote-rclone | git-annex-remote-rclone | (忽略) | ⏭️  | 在忽略列表 |
| nfs-common | nfs-common | nfs-utils | ❌ | **映射失败** |

**结论**: 
- ✅ `pkg-config` 映射成功（`pkgconf-pkg-config` 已安装）
- ❌ `libfuse-dev` 未被映射到 `fuse3-devel`
- ❌ `nfs-common` 未被映射到 `nfs-utils`

### 2.2 编译失败的直接原因

**错误信息**:
```
fatal error: fuse.h: No such file or directory
```

**原因链**:
```
apt-get install libfuse-dev 
    ↓ (apt-get wrapper 应该调用)
convert_package_names ("libfuse-dev")
    ↓ (查询 package-map.json)
get_package_mapping ("libfuse-dev")
    ↓ (期望返回 "fuse3-devel")
❌ 返回空字符串 或 "libfuse-dev" 本身
    ↓ (apt-get wrapper 继续)
dnf install -y libfuse-dev
    ↓ (libfuse-dev 是 Ubuntu 包名，Rocky 不存在)
包管理器错误: "未找到匹配的参数: libfuse-dev"
    ↓ (libfuse-dev 没有被安装)
pkg-config 因此也找不到 libfuse
    ↓
C 编译器找不到 <fuse.h> 头文件
    ↓
❌ 编译失败
```

---

## 3. 诊断结论

### 问题根源分析

#### 假设 A: apt-get wrapper 未被调用 ❓
**证据比对**:
- ✅ `pkg-config` 成功映射了 → wrapper 肯定被调用了
- ✅ `fuse3` 成功安装 → wrapper 肯定在执行
- 结论: **wrapper 已被调用**，但**映射不完整**

#### 假设 B: package-map.json 配置不完整 ✓ 高可能性
**证据**:
- ✅ setup 脚本中包含 `"libfuse-dev": "fuse3-devel"` 定义
- ✅ 但 `pkg-config` 映射成功表明 jq 和配置文件本身都工作正常
- ❓ 为什么 libfuse-dev 没有被映射?

**可能的原因**:
1. **配置文件生成顺序问题**: 
   - 如果 setup 脚本运行时发生网络中断或权限问题
   - package-map.json 创建可能失败或不完整

2. **jq 查询失败**:
   - jq 版本问题可能导致某些键查询失败
   - JSON 格式错误

3. **键名匹配问题**:
   - libfuse-dev -> 可能被转义为其他形式
   - JSON 中的键名混乱

#### 假设 C: 工作流条件问题 ⚠️
**观察**:
```yaml
if: matrix.os == 'ubuntu-latest'
```
- `matrix.os` 设置为 `ubuntu-latest` 以便配置编译选项
- 但 `runs-on` 实际为 `self-hosted`（Rocky Linux）
- **工作流条件检查的是 matrix 变量，而非实际运行器**

**影响**: 
- 库安装步骤因 `matrix.os == 'ubuntu-latest'` 条件被执行 ✅
- 但在 Rocky 自托管运行器上执行 ❌
- 预期 Ubuntu 的 apt-get，但在 Rocky 上运行

---

## 4. 核心问题判断

### 最可能的原因排序

1. **🔴 PRIMARY: libfuse-dev 映射失败**
   - `pkg-config` 映射成功证明系统工作
   - 仅有的解释: package-map.json 中缺少或损坏了 libfuse-dev 条目

2. **🟡 SECONDARY: jq 查询问题**
   - JSON 键的转义或编码问题
   - 特殊字符（-）可能导致查询失败

3. **🟠 TERTIARY: 配置文件未正确生成**
   - setup 脚本可能在 package-map.json 生成时失败
   - 智能合并逻辑可能有 bug

---

## 5. 缺失的诊断信息

要对问题下最终结论，需要以下数据：

### 5.1 必需信息
```bash
# Rocky 运行器上需要检查:
cat /opt/actions-runner/compat-scripts/package-map.json | jq .
```
**为什么重要**: 确认 libfuse-dev 映射是否存在且格式正确

```bash
# 检查 jq 是否可用:
which jq && jq --version
```
**为什么重要**: 确认依赖工具是否可用

```bash
# 测试映射查询:
jq '.["libfuse-dev"]' /opt/actions-runner/compat-scripts/package-map.json
```
**为什么重要**: 验证特定键的查询是否工作

### 5.2 补充信息
```bash
# 查看 apt-get wrapper 完整执行:
/opt/actions-runner/compat-scripts/apt-get install -y libfuse-dev 2>&1 | tee /tmp/apt-wrapper.log
```

```bash
# 检查 dnf 是否知道 fuse3-devel:
dnf search fuse3-devel
```

---

## 6. 三个最可能的情景

### 情景 1: 映射表中 libfuse-dev KEY 不存在
```json
{
  "_version": "3.2",
  "fuse3": "fuse3",           // 用户可能手动修改导致
  "nfs-common": "nfs-utils",
  // ❌ libfuse-dev 条目缺失或被删除
  "pkg-config": "pkgconf-pkg-config"
}
```
**解决方案**: 重新添加 `"libfuse-dev": "fuse3-devel"`

### 情景 2: JSON 格式错误导致 jq 查询失败
```json
{
  "libfuse-dev" "fuse3-devel",  // ❌ 缺少冒号
  "pkg-config": "pkgconf-pkg-config"
}
```
**症状**: jq 会静默失败，返回空值  
**解决方案**: 修复 JSON 语法

### 情景 3: 配置文件版本升级导致合并失败
```
旧版本 (v3.1): 有 libfuse-dev 映射
新运行 setup (v3.2): 智能合并逻辑有 bug
结果: libfuse-dev 消失
```
**线索**: 应检查 `/opt/actions-runner/compat-scripts/package-map.json.bak`（备份文件）  
**解决方案**: 检查 jq 合并逻辑的完整性

---

## 7. 建议的修复步骤（不实施）

### 方案 A: 手动验证和修复（临时）
```bash
# 1. 在 Rocky runner 上执行
ssh runner@Rocky-KVM

# 2. 验证映射表
cat /opt/actions-runner/compat-scripts/package-map.json

# 3. 测试 libfuse-dev 查询
jq '.["libfuse-dev"]' /opt/actions-runner/compat-scripts/package-map.json

# 4. 如果为空，手动修复
jq '.["libfuse-dev"] = "fuse3-devel"' \
  /opt/actions-runner/compat-scripts/package-map.json \
  > /tmp/package-map.json.new && \
  mv /tmp/package-map.json.new \
  /opt/actions-runner/compat-scripts/package-map.json

# 5. 验证修复
jq -r 'to_entries[] | select(.key | startswith("_") | not)' \
  /opt/actions-runner/compat-scripts/package-map.json

# 6. 重新运行工作流
```

### 方案 B: 脚本自诊断（需要在工作流中集成）
```bash
# 在 workflow 的 "Install Libraries" 步骤前添加诊断步骤
- name: Diagnose apt-get wrapper
  if: matrix.os == 'ubuntu-latest'
  run: |
    echo "=== apt-get wrapper 诊断 ==="
    /opt/actions-runner/compat-scripts/apt-get config-info
    echo ""
    echo "=== 测试 libfuse-dev 映射 ==="
    jq '.["libfuse-dev"]' /opt/actions-runner/compat-scripts/package-map.json || echo "查询失败"
```

### 方案 C: setup 脚本改进（需要编码）
在 `setup-rocky-9.4-ci-env-v3.1.sh` 中添加：
```bash
# 验证映射表完整性
validate_package_map() {
    local required_mappings=("libfuse-dev" "nfs-common" "pkg-config")
    for mapping in "${required_mappings[@]}"; do
        if ! jq -e ".\"$mapping\"" "$PACKAGE_MAP_FILE" >/dev/null 2>&1; then
            print_error "❌ 映射表缺失: $mapping"
            return 1
        fi
    done
    print_success "✅ 映射表完整"
    return 0
}
```

---

## 8. 次要作业失败

### linux_386 (64272764517) 作业
- **状态**: 运行达 9m51s，在 "Run tests" 步骤失败
- **原因**: 依赖于 linux 作业的编译产物不可用（上游失败）
- **表现**: 测试执行时找不到编译后的二进制文件

### Windows / other_os / go1.24 / mac 作业  
- **状态**: 立即失败或快速返回
- **原因**: 这些作业不受 libfuse-dev 影响
- **真实原因**: 需要查看各自的详细日志，但可能与：
  - 自托管运行器对其他操作系统的支持
  - 工作流逻辑（lint 和 android-all 被跳过表明构建脓可能提前终止）

---

## 9. 根本原因总结

```
┌─────────────────────────────────────────┐
│  GitHub Actions Start (Run 22220002065) │
└────────────────────┬────────────────────┘
                     │
         ┌───────────┴───────────┐
         │                       │
   ┌──────────────┐      ┌──────────────────┐
   │ Linux Job    │      │ Other OS Jobs    │
   │ (main issue) │      │ (dependent)      │
   └──────┬───────┘      └────────┬─────────┘
          │                       │
    ┌─────────────────────┐       │
    │ Install Libraries  │       │
    │ (matrix.os check)  │       │
    └────────┬────────────┘       │
             │                    │
    ┌────────────────────────┐    │
    │ apt-get install ...    │    │
    │ - fuse3 ✅            │    │
    │ - libfuse-dev ❌      │    │
    │ - rpm ✅              │    │
    │ - nfs-common ❌       │    │
    └─────────┬──────────────┘    │
              │                   │
    ┌─────────────────────────┐   │
    │  Build rclone           │   │
    │                         │   │
    │  pkg-config --cflags    │   │ (Cascading)
    │  looking for libfuse    │   │ (Failure)
    │                         │   │
    │  gcc unable to find     │   │
    │  <fuse.h>              │   │
    │                         │   │
    │  ❌ COMPILATION FAILED  │   │
    └──────────┬──────────────┘   │
               │                  │
               └───────┬──────────┘
                       │
         ┌─────────────────────────┐
         │  All Jobs Fail/Cancel   │
         │  (User cancels run)     │
         └─────────────────────────┘
```

### 最终结论

**主要问题**: `libfuse-dev` 未被成功映射到 Rocky 的 `fuse3-devel` 包  

**立即影响**: `fuse.h` 头文件未能安装 → 编译失败 → 所有作业失败  

**根本原因**: 三个可能性（按可能性排序）：
1. package-map.json 中 libfuse-dev 映射条目缺失或损坏
2. jq 查询因 JSON 格式错误而失败  
3. setup 脚本配置文件生成/合并阶段出错

**需要的诊断步骤**: 直接检查运行器上的 `/opt/actions-runner/compat-scripts/package-map.json` 文件内容

---

## 10. 后续建议

### 短期行动（48 小时内）
1. ✅ 分析本报告中的诊断信息需求
2. ⏳ SSH 登录 Rocky-KVM 运行器，手动运行诊断命令
3. ⏳ 收集 package-map.json 的实际内容
4. ⏳ 确认真实原因

### 中期行动（1 周内）
1. 实施 package-map.json 的自我修复机制
2. 在 setup 脚本中添加验证函数，确保全部必需映射存在
3. 在工作流中添加诊断步骤，便于快速定位问题

### 长期改进
1. 创建自动化测试，验证所有必需包的映射
2. 在工作流失败时自动运行诊断脚本
3. 实现 package-map.json 的版本管理和回滚机制

